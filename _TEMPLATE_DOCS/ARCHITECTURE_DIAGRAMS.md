# 🏗️ ARQUITETURA VISUAL - Template BFF Supabase

> **Diagramas e visualizações** da arquitetura do template

---

## 📊 VISÃO GERAL DA STACK

```
┌─────────────────────────────────────────────────────────────┐
│                         USUÁRIO                              │
│                    (Browser/Mobile)                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      NEXT.JS 16+                             │
│                    (BFF - App Router)                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  CLIENT COMPONENTS                                     │ │
│  │  • Interatividade (useState, useEffect)                │ │
│  │  • Eventos de usuário                                  │ │
│  │  • Client-side validations                             │ │
│  └────────────────────────────────────────────────────────┘ │
│                         │                                    │
│                         ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  SERVER COMPONENTS                                     │ │
│  │  • Fetch de dados                                      │ │
│  │  • SEO                                                 │ │
│  │  • Renderização server-side                            │ │
│  └────────────────────────────────────────────────────────┘ │
│                         │                                    │
│                         ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  SERVER ACTIONS ('use server')                         │ │
│  │  • Validação de autenticação                           │ │
│  │  • Validação de autorização (roles)                    │ │
│  │  • Lógica de negócio                                   │ │
│  │  • Mutations de dados                                  │ │
│  └────────────────────────────────────────────────────────┘ │
│                         │                                    │
│                         ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  MIDDLEWARE                                            │ │
│  │  • Validação de sessão                                 │ │
│  │  • Proteção de rotas                                   │ │
│  │  • Refresh de tokens                                   │ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Supabase SDK
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      SUPABASE                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  AUTHENTICATION                                        │ │
│  │  • JWT tokens                                          │ │
│  │  • Session management                                  │ │
│  │  • Email/Password, OAuth, Magic Links                  │ │
│  └────────────────────────────────────────────────────────┘ │
│                         │                                    │
│                         ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  POSTGRESQL DATABASE                                   │ │
│  │  • Dados estruturados                                  │ │
│  │  • Relacionamentos                                     │ │
│  │  • Transações ACID                                     │ │
│  └────────────────────────────────────────────────────────┘ │
│                         │                                    │
│                         ▼                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ROW LEVEL SECURITY (RLS)                              │ │
│  │  • Isolamento por tenant_id                            │ │
│  │  • Policies baseadas em roles                          │ │
│  │  • Segurança a nível de linha                          │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 CAMADAS DE SEGURANÇA

```
┌─────────────────────────────────────────────────────┐
│  CAMADA 1: MIDDLEWARE                               │
│  ────────────────────────────────────────────────   │
│  Valida sessão antes de chegar na rota             │
│  • Verifica JWT token válido                        │
│  • Redirect não-autenticados → /login              │
│  • Refresh automático de tokens                     │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  CAMADA 2: ROLE GUARD (UI)                          │
│  ────────────────────────────────────────────────   │
│  Protege rotas baseado em roles                     │
│  • Verifica role do usuário                         │
│  • Mostra/esconde componentes                       │
│  • Redirect não-autorizado → /access-blocked        │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  CAMADA 3: SERVER ACTIONS                           │
│  ────────────────────────────────────────────────   │
│  Valida permissões antes de executar ação           │
│  • getCurrentUserProfile()                          │
│  • Verifica role permitida                          │
│  • Throw error se não autorizado                    │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  CAMADA 4: RLS (DATABASE)                           │
│  ────────────────────────────────────────────────   │
│  Última linha de defesa                             │
│  • Filtra rows por tenant_id                        │
│  • Políticas SQL impossíveis de bypassar            │
│  • Funciona mesmo se outras camadas falharem        │
└─────────────────────────────────────────────────────┘
```

---

## 🏢 MULTI-TENANCY ARCHITECTURE

```
┌───────────────────────────────────────────────────────────┐
│                     PLATAFORMA SAAS                        │
│                    (SUPERADMIN view)                       │
└───────────────────────────────────────────────────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│   TENANT A       │ │   TENANT B       │ │   TENANT C       │
│  (gym_id: 001)   │ │  (gym_id: 002)   │ │  (gym_id: 003)   │
├──────────────────┤ ├──────────────────┤ ├──────────────────┤
│ ┌──────────────┐ │ │ ┌──────────────┐ │ │ ┌──────────────┐ │
│ │   ADMIN      │ │ │ │   ADMIN      │ │ │ │   ADMIN      │ │
│ └──────────────┘ │ │ └──────────────┘ │ │ └──────────────┘ │
│        │         │ │        │         │ │        │         │
│   ┌────┴────┐    │ │   ┌────┴────┐    │ │   ┌────┴────┐    │
│   ▼         ▼    │ │   ▼         ▼    │ │   ▼         ▼    │
│ MANAGER  MANAGER │ │ MANAGER  MANAGER │ │ MANAGER  MANAGER │
│   │         │    │ │   │         │    │ │   │         │    │
│   ▼         ▼    │ │   ▼         ▼    │ │   ▼         ▼    │
│ USERS    USERS   │ │ USERS    USERS   │ │ USERS    USERS   │
└──────────────────┘ └──────────────────┘ └──────────────────┘

RLS POLICY: WHERE tenant_id = (SELECT tenant_id FROM users WHERE id = auth.uid())
           OR EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'SUPERADMIN')
```

---

## 📂 ESTRUTURA DE DADOS

```
┌─────────────────────────────────────────────────────────────┐
│                      DATABASE SCHEMA                         │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐
│   auth.users     │         │     tenants      │
│  (Supabase)      │         │   (sua app)      │
├──────────────────┤         ├──────────────────┤
│ id (PK)          │◄────┐   │ id (PK)          │
│ email            │     │   │ name             │
│ encrypted_pass   │     │   │ slug (unique)    │
│ created_at       │     │   │ is_active        │
└──────────────────┘     │   │ created_at       │
                         │   └──────────────────┘
                         │            ▲
                         │            │
                         │            │ FK: tenant_id
┌──────────────────┐     │            │
│      users       │     │            │
│   (sua app)      │     │            │
├──────────────────┤     │            │
│ id (PK) ─────────┼─────┘            │
│ tenant_id (FK) ──┼──────────────────┘
│ email            │
│ name             │
│ role (ENUM)      │
│ created_at       │
└──────────────────┘
         │
         │ FK: tenant_id
         ▼
┌──────────────────┐
│ your_entities    │
│  (domain)        │
├──────────────────┤
│ id (PK)          │
│ tenant_id (FK) ──┼──► SEMPRE incluir!
│ user_id (FK)     │
│ ...campos...     │
│ created_at       │
└──────────────────┘
```

---

## 🔄 FLUXO DE AUTENTICAÇÃO

```
┌─────────┐
│ USUÁRIO │
└────┬────┘
     │
     │ 1. Acessa /dashboard
     ▼
┌──────────────────┐
│   MIDDLEWARE     │ 2. Intercepta request
├──────────────────┤
│ • Verifica cookie│
│ • Valida JWT     │
└────┬────┬────────┘
     │    │
     │    │ 3a. Não autenticado
     │    └────────────────────┐
     │                         │
     │ 3b. Autenticado         │
     ▼                         ▼
┌──────────────────┐    ┌──────────────────┐
│   DASHBOARD      │    │   /LOGIN         │
│                  │    │                  │
│ 4. Server Comp   │    │ 5. Formulário    │
│    busca dados   │    │                  │
└────┬─────────────┘    └────┬─────────────┘
     │                       │
     │ 5. getCurrentUser()   │ 6. submit (Server Action)
     ▼                       ▼
┌──────────────────┐    ┌──────────────────┐
│   SUPABASE       │    │   SUPABASE       │
│   auth.getUser() │    │   auth.signIn()  │
└────┬─────────────┘    └────┬─────────────┘
     │                       │
     │ 6. SELECT users       │ 7. Cria sessão + cookie
     ▼                       ▼
┌──────────────────┐    ┌──────────────────┐
│   users table    │    │   auth.users +   │
│   (com RLS)      │    │   users table    │
└──────────────────┘    └──────────────────┘
                              │
                              │ 8. redirect('/dashboard')
                              ▼
                        ┌──────────────────┐
                        │   DASHBOARD      │
                        │   (autenticado)  │
                        └──────────────────┘
```

---

## 🎯 FLUXO DE SERVER ACTION

```
┌─────────────────┐
│ CLIENT COMPONENT│
│  (formulário)   │
└────┬────────────┘
     │
     │ 1. onSubmit (form data)
     ▼
┌────────────────────────────────────────┐
│ SERVER ACTION ('use server')           │
├────────────────────────────────────────┤
│ 2. Validar autenticação                │
│    const user = await                  │
│      getCurrentUserProfile()           │
│                                        │
│    if (!user) throw Error()            │
├────────────────────────────────────────┤
│ 3. Validar autorização (role)          │
│    if (!allowedRoles.includes(role))   │
│      throw Error('Unauthorized')       │
├────────────────────────────────────────┤
│ 4. Validar inputs                      │
│    if (!data.field) return error       │
├────────────────────────────────────────┤
│ 5. Executar lógica de negócio          │
│    const result = await supabase       │
│      .from('table')                    │
│      .insert({                         │
│        ...data,                        │
│        tenant_id: user.tenant_id  ◄─── SEMPRE!
│      })                                │
├────────────────────────────────────────┤
│ 6. Revalidar cache                     │
│    revalidatePath('/path')             │
├────────────────────────────────────────┤
│ 7. Retornar resultado                  │
│    return { success: true, data }      │
└────┬───────────────────────────────────┘
     │
     │ 8. Atualizar UI
     ▼
┌─────────────────┐
│ CLIENT COMPONENT│
│  (atualizado)   │
└─────────────────┘
```

---

## 🛡️ RLS POLICY FLOW

```
┌─────────────┐
│   USUÁRIO   │
│ (autenticado)
└──────┬──────┘
       │
       │ auth_uid = 'user-123'
       │ tenant_id = 'tenant-abc'
       │ role = 'ADMIN'
       ▼
┌──────────────────────────────────┐
│ SELECT * FROM entities           │
│ WHERE category = 'example'       │
└──────┬───────────────────────────┘
       │
       │ PostgreSQL adiciona automaticamente:
       ▼
┌──────────────────────────────────┐
│ SELECT * FROM entities           │
│ WHERE category = 'example'       │
│ AND (                            │  ◄─── RLS POLICY!
│   tenant_id IN (                 │
│     SELECT tenant_id             │
│     FROM users                   │
│     WHERE id = auth.uid()        │  ◄─── Retorna 'tenant-abc'
│   )                              │
│   OR                             │
│   EXISTS (                       │
│     SELECT 1 FROM users          │
│     WHERE id = auth.uid()        │
│     AND role = 'SUPERADMIN'      │  ◄─── Se SUPERADMIN, vê tudo
│   )                              │
│ )                                │
└──────┬───────────────────────────┘
       │
       │ Retorna apenas rows com:
       │ tenant_id = 'tenant-abc'
       ▼
┌──────────────────────────────────┐
│ ✅ entity-1 (tenant: abc)        │
│ ✅ entity-2 (tenant: abc)        │
│ ❌ entity-3 (tenant: xyz) HIDDEN │
│ ❌ entity-4 (tenant: xyz) HIDDEN │
└──────────────────────────────────┘
```

---

## 📱 COMPONENTE HIERARCHY

```
┌─────────────────────────────────────────────────┐
│ app/layout.tsx (Root Layout)                    │
│ • Metadata                                      │
│ • Fonts                                         │
│ • Global styles                                 │
└────┬────────────────────────────────────────────┘
     │
     ├─► ┌─────────────────────────────────────┐
     │   │ middleware.ts                       │
     │   │ • Valida sessão                     │
     │   │ • Protege rotas                     │
     │   └─────────────────────────────────────┘
     │
     ├─► ┌─────────────────────────────────────┐
     │   │ app/login/page.tsx                  │
     │   │ (Public Route)                      │
     │   └─────────────────────────────────────┘
     │
     ├─► ┌─────────────────────────────────────┐
     │   │ app/admin/layout.tsx                │
     │   │ ┌─────────────────────────────────┐ │
     │   │ │ <RoleGuard                      │ │
     │   │ │   allowedRoles={['ADMIN']} >    │ │
     │   │ │   Header + Nav + Content        │ │
     │   │ │ </RoleGuard>                    │ │
     │   │ └─────────────────────────────────┘ │
     │   └────┬────────────────────────────────┘
     │        │
     │        ├─► app/admin/page.tsx (Dashboard)
     │        ├─► app/admin/users/page.tsx
     │        └─► app/admin/settings/page.tsx
     │
     ├─► ┌─────────────────────────────────────┐
     │   │ app/manager/layout.tsx              │
     │   │ ┌─────────────────────────────────┐ │
     │   │ │ <RoleGuard                      │ │
     │   │ │   allowedRoles={['MANAGER']} >  │ │
     │   │ │   Header + Nav + Content        │ │
     │   │ │ </RoleGuard>                    │ │
     │   │ └─────────────────────────────────┘ │
     │   └────┬────────────────────────────────┘
     │        │
     │        ├─► app/manager/page.tsx
     │        └─► app/manager/team/page.tsx
     │
     └─► ┌─────────────────────────────────────┐
         │ app/user/layout.tsx                 │
         │ ┌─────────────────────────────────┐ │
         │ │ <RoleGuard                      │ │
         │ │   allowedRoles={['USER']} >     │ │
         │ │   Header + Nav + Content        │ │
         │ │ </RoleGuard>                    │ │
         │ └─────────────────────────────────┘ │
         └────┬────────────────────────────────┘
              │
              └─► app/user/page.tsx
```

---

## 🗂️ FILE ORGANIZATION

```
project/
│
├── 🎨 FRONTEND
│   ├── app/                    Routes (App Router)
│   │   ├── layout.tsx         Root layout
│   │   ├── page.tsx           Home
│   │   ├── login/             Public routes
│   │   └── [role]/            Protected routes by role
│   │       ├── layout.tsx     ← RoleGuard aqui
│   │       └── [feature]/     Features da role
│   │
│   └── components/             React components
│       ├── role-guard.tsx     HOC de autorização
│       ├── [feature]/         Agrupado por feature
│       └── ui/                Genéricos
│
├── 🔧 BACKEND (BFF Layer)
│   ├── lib/
│   │   ├── supabase/          Clientes Supabase
│   │   │   ├── client.ts      Browser client
│   │   │   ├── server.ts      Server client + admin
│   │   │   └── middleware.ts  Session management
│   │   │
│   │   ├── actions/           Server Actions
│   │   │   ├── admin-actions.ts
│   │   │   ├── user-actions.ts
│   │   │   └── common-actions.ts
│   │   │
│   │   └── helpers/           Utility functions
│   │       ├── user.ts
│   │       ├── validation.ts
│   │       └── index.ts
│   │
│   └── middleware.ts          Global middleware
│
├── 📊 DATABASE
│   └── supabase/
│       ├── migrations/        SQL migrations
│       │   ├── 001_initial.sql
│       │   ├── 002_rls.sql
│       │   └── 003_feature.sql
│       │
│       └── seed.sql           Initial data
│
├── 📘 TYPES
│   └── types/
│       ├── database.ts        DB types
│       └── ui.ts             UI types
│
└── 📚 DOCS
    ├── specs/                Features specs
    └── docs/                 Additional docs
```

---

## 🔀 REQUEST LIFECYCLE

```
1. USER ACTION
   ↓
2. BROWSER
   • Client Component
   • Event handler (onClick, onSubmit)
   ↓
3. MIDDLEWARE
   • Intercepta request
   • Valida sessão JWT
   • Refresh token se necessário
   • Permite ou redireciona
   ↓
4. ROUTE
   • App Router match
   • Layout carregado (com RoleGuard)
   ↓
5. SERVER COMPONENT
   • Renderiza no servidor
   • Faz fetch de dados
   • Retorna HTML
   ↓
6. CLIENT HYDRATION
   • JavaScript carrega
   • React hydrate
   • Componentes interativos
   ↓
7. SERVER ACTION (se mutation)
   • 'use server'
   • Validações
   • Supabase query (com RLS)
   • Revalidate cache
   • Return result
   ↓
8. UI UPDATE
   • Re-render automático
   • Loading states
   • Error boundaries
```

---

## 🎯 DECISION TREE: Server vs Client Component

```
                    Preciso usar hooks
                   (useState, useEffect)?
                            │
                ┌───────────┴───────────┐
                │                       │
               SIM                     NÃO
                │                       │
                ▼                       ▼
        ╔═══════════════╗     Preciso acessar
        ║    CLIENT     ║     dados do banco?
        ║  COMPONENT    ║              │
        ║ ('use client')║    ┌─────────┴──────────┐
        ╚═══════════════╝    │                    │
                            SIM                  NÃO
                             │                    │
                             ▼                    ▼
                    É mutação/ação?      ╔═══════════════╗
                             │           ║    SERVER     ║
                  ┌──────────┴─────┐    ║  COMPONENT    ║
                  │                │    ║  (default)    ║
                 SIM              NÃO   ╚═══════════════╝
                  │                │
                  ▼                ▼
         ╔═══════════════╗  ╔═══════════════╗
         ║ SERVER ACTION ║  ║    SERVER     ║
         ║ ('use server')║  ║  COMPONENT    ║
         ╚═══════════════╝  ║  + fetch      ║
                            ╚═══════════════╝
```

---

## 📈 SCALABILITY MODEL

```
SINGLE TENANT (Início)
┌─────────────┐
│  Tenant A   │
│  100 users  │
└─────────────┘
      │
      │ Same code,
      │ same deploy
      ▼
MULTI TENANT (Crescimento)
┌─────────────┬─────────────┬─────────────┐
│  Tenant A   │  Tenant B   │  Tenant C   │
│  500 users  │  300 users  │  200 users  │
└─────────────┴─────────────┴─────────────┘
      │
      │ Horizontal
      │ scaling
      ▼
ENTERPRISE (Escala)
┌─────────────┬─────────────┬─────────────┬─────────────┐
│  Tenant A   │  Tenant B   │  Tenant C   │  Tenant N   │
│  5k users   │  3k users   │  2k users   │  1k users   │
└─────────────┴─────────────┴─────────────┴─────────────┘

                    🔒 RLS garante isolamento
                    📊 Um banco para todos
                    🚀 Deploy único
                    💰 Custo otimizado
```

---

**Última atualização**: Fevereiro 2026  
**Versão**: 1.0.0  
**Documento técnico**: Para referência rápida e onboarding
